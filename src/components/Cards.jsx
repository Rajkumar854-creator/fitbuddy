import { Link } from 'react-router-dom';
import { ArrowUpRight, Check, Clock3, Dumbbell, Play } from 'lucide-react';
export function StatCard({label,value,detail,icon:Icon}){ return <div className="stat-card"><div className="stat-icon"><Icon size={18}/></div><span>{label}</span><strong>{value}</strong><small>{detail}</small></div> }
export function WorkoutCard({workout,completed=false,onComplete}){ return <article className={`workout-card ${completed?'done':''}`}><div className="workout-top"><span className="eyebrow">{workout.day}</span>{completed?<span className="status complete"><Check size={14}/> Completed</span>:<span className="status">Upcoming</span>}</div><h3>{workout.title}</h3><div className="workout-meta"><span><Clock3 size={15}/>{workout.duration} min</span><span><Dumbbell size={15}/>{workout.exercises.length} exercises</span></div><div className="workout-actions"><Link className="text-link" to={`/workout/${workout.id}`}>View details <ArrowUpRight size={15}/></Link>{onComplete&&<button className="round-action" onClick={onComplete} aria-label="Complete workout"><Check size={17}/></button>}</div></article> }
export function PlanCard({plan,onUse}){ return <article className="sample-card"><div className="sample-art"><span>{plan.days} sessions / week</span><strong>{plan.name.split(' ').map(word=>word[0]).join('')}</strong></div><div className="sample-content"><span className="eyebrow">{plan.goal} · {plan.difficulty}</span><h3>{plan.name}</h3><p>{plan.description}</p><div className="sample-meta"><span>{plan.duration} min</span><span>{plan.equipment}</span></div><button className="text-link" onClick={onUse}>Use this plan <Play size={14}/></button></div></article> }
